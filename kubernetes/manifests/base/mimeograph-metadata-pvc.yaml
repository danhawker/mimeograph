apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mimeograph-metadata
  namespace: mimeograph
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: patch_me
  resources:
    requests:
      storage: 10Gi